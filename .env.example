# Supabase project the app talks to. Both values are public by design (they ship
# in the browser bundle); the service-role key never appears here.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-publishable-key
